{{- if .Values.datasync.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: datasync
  labels:
    io.kompose.service: datasync
spec:
  selector:
    io.kompose.service: datasync
status:
  loadBalancer: {}
{{- end }}